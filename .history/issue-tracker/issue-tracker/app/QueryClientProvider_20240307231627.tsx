import React from "react";

import { QueryClient, QueryClientProvider as React } from "@tanstack/react-query";

const QueryClientProvider = () => {
  return <div>QueryClientProvider</div>;
};

export default QueryClientProvider;
